#!/bin/sh
# aube-bin-shim v1 target=../../bin/ensure-hopjs.mjs
basedir=$(dirname "$0")
export NODE_PATH="$basedir/..:$basedir/../.aube/node_modules"
if [ -x "$basedir/node" ]; then
  exec "$basedir/node" "$basedir/../../bin/ensure-hopjs.mjs" "$@"
else
  exec node "$basedir/../../bin/ensure-hopjs.mjs" "$@"
fi
